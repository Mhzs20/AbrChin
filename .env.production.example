# Production placeholders only — never commit real secrets
DATABASE_URL=postgresql://USER:PASSWORD@db:5432/abrchin?schema=public
SESSION_SECRET=
# 32 random bytes encoded as base64; keep outside git and application-data backups.
CREDENTIAL_ENCRYPTION_KEY=

# Connection Check validates configuration only; it never sends a test OTP.
SMS_PROVIDER=kavenegar
KAVENEGAR_API_KEY=
KAVENEGAR_TEMPLATE=abrchinlogin
KAVENEGAR_ALERT_TEMPLATE=abrchinops
KAVENEGAR_TIMEOUT_MS=8000

# Must be an HTTPS public origin; Connection Check never creates a payment.
PAYMENT_CALLBACK_BASE_URL=https://abrchin.ir
PAYMENT_BOOTSTRAP_DEFAULT_PROVIDER=zibal

ZIBAL_MERCHANT=
ZIBAL_TIMEOUT_MS=10000

ZARINPAL_MERCHANT_ID=
ZARINPAL_SANDBOX=false
ZARINPAL_TIMEOUT_MS=10000

# Email verification (production SMTP). Never commit real credentials.
# Runtime source of truth is the production .env / .env.production on the host.
EMAIL_PROVIDER=smtp
EMAIL_FROM=
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_TIMEOUT_MS=10000
EMAIL_VERIFICATION_TTL_SECONDS=600

ADMIN_MOBILES=

OTP_TTL_SECONDS=120
SESSION_TTL_DAYS=30

# MessageGo product credential. Provider API keys remain only on MessageGo.
MESSAGEGO_BASE_URL=https://aichin.abrchin.ir
MESSAGEGO_CLIENT_ID=abrchin-production
MESSAGEGO_CLIENT_SECRET=
MESSAGEGO_TENANT_ID=abrchin
MESSAGEGO_WORKSPACE_ID=production
MESSAGEGO_TIMEOUT_MS=15000

# Reverse proxy trust (typically 1 behind nginx)
TRUSTED_PROXY_HOPS=1

# Infrastructure provider: mock only (ArvanCloud uses its own adapter)
INFRASTRUCTURE_PROVIDER_MODE=mock
PUBLIC_SALE_ENABLED=true

ARVAN_ENABLED=false
ARVAN_API_KEY=
ARVAN_API_BASE_URL=https://napi.arvancloud.ir/ecc/v1
ARVAN_API_VERSION=v1
ARVAN_REGION_CODES=ir-thr-si1,ir-thr-fr1,ir-tbz-sh1,ir-thr-ba1,ir-southwest1-a,eu-west1-a
ARVAN_TIMEOUT_MS=15000
ARVAN_GET_ATTEMPTS=3
ARVAN_PUBLIC_SALE_ENABLED=true
ARVAN_READY_PUBLIC_SALE_ENABLED=true
ARVAN_CLOUD_PUBLIC_SALE_ENABLED=true
MANUAL_READY_PUBLIC_SALE_ENABLED=true
ARVAN_MUTATIONS_ENABLED=false
CATALOG_SYNC_INTERVAL_MS=300000

# Provisioning worker (never pass ARVAN_API_KEY as build arg)
WORKER_POLL_MS=3000
WORKER_LEASE_MS=120000
WORKER_STALE_AFTER_MS=60000
WORKER_ID=provisioning-worker
# Retry-safe hourly/daily PAYG settlement scheduler.
BILLING_WORKER_INTERVAL_MS=60000

POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
ABRCHIN_IMAGE=
# Keep false in steady-state production; ops/deploy.sh runs an explicit migrate gate.
ABRCHIN_RUN_MIGRATE_ON_START=false
